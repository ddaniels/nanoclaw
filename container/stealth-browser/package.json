{
  "name": "stealth-browser",
  "version": "0.1.0",
  "private": true,
  "description": "Sidecar to agent-browser. Uses rebrowser-playwright (Playwright fork with rebrowser-patches) to drive Chromium with stealth evasions for sites that fingerprint stock automation. Used inside the NanoClaw container when the target domain has a saved login captured by /add-site-login.",
  "type": "module",
  "bin": {
    "stealth-browser": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepack": "tsc"
  },
  "dependencies": {
    "minimist": "1.2.8",
    "playwright-extra": "4.3.6",
    "puppeteer-extra-plugin-stealth": "2.11.2",
    "rebrowser-playwright": "1.52.0"
  },
  "devDependencies": {
    "@types/minimist": "1.2.5",
    "@types/node": "22.10.0",
    "typescript": "5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
