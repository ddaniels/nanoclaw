OLLAMA_HOST=

# Local browser MCP tool — only set if Chrome is reachable at a non-default
# address. Defaults: host.docker.internal:9222 (works on Docker Desktop with
# the /add-local-browser-tool plist installed).
LOCAL_BROWSER_CDP_HOST=
LOCAL_BROWSER_CDP_PORT=
